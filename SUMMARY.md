Art Director (định hướng thẩm mỹ) và tinh chỉnh cảm giác (feel).
Còn AI (Codex, Gemini, Claude) đóng vai trò thợ toán học — giúp bạn viết mã GLSL, tính toán tọa độ 3D và dựng khung Three.js cực kỳ nhanh chóng.

Three.js / WebGL / React Three Fiber: Đây là nền tảng để đưa đồ họa 3D lên trình duyệt web. Three.js giúp bạn tạo scene, camera, ánh sáng và render các mô hình 3D phức tạp mà không cần viết WebGL thuần túy từ đầu. GLSL (Shaders): Kỹ năng viết mã đổ bóng (shader) cho GPU. Đây là "vũ khí bí mật" để tạo ra các hiệu ứng méo mó (distortion), mặt nước lượn sóng, hạt (particles) chuyển động, hoặc làm chảy hình ảnh khi người dùng cuộn chuột. GSAP (GreenSock): Thư viện mạnh nhất hiện nay để quản lý timeline animation và hiệu ứng kích hoạt khi cuộn (ScrollTrigger). GSAP có thể đồng bộ hóa hoạt ảnh của các object 3D trong Three.js với DOM HTML thông thường. Smooth Scrolling (Lenis / Locomotive Scroll): Ghi đè thanh cuộn mặc định của trình duyệt để tạo cảm giác cuộn mượt mà (smooth scroll), giúp các hiệu ứng parallax 3D không bị giật lag.
